## 1. 平台目录模型

- [x] 1.1 在独立前端模块中定义知识分类、项目类型和默认元数据。
- [x] 1.2 扩展 `WikiProject` 元数据类型，同时兼容没有元数据的旧项目。
- [x] 1.3 增加项目元数据读写辅助函数，把元数据存在项目本地，并将缺失值归一化为 `general`。
- [x] 1.4 更新最近项目和最后打开项目持久化逻辑，使可用元数据被保留，缺失元数据时安全降级。

## 2. 业务项目创建

- [x] 2.1 更新创建项目弹窗，支持选择分类、项目类型、业务上下文和试点/自用试点状态。
- [x] 2.2 增加业务模板：标注、爬虫、自用试点/黑客松试点、项目资产和通用项目。
- [x] 2.3 根据所选业务元数据初始化 `purpose.md` 和 `schema.md`，不修改现有知识提取管线。
- [x] 2.4 确保黑客松自用试点项目只使用普通项目元数据，不增加黑客松专用应用分支。

## 3. 平台首页体验

- [x] 3.1 将扁平最近项目首屏改为按业务分类和项目类型分组的知识库目录。
- [x] 3.2 保留现有最近项目访问和移除行为。
- [x] 3.3 为没有项目的分类增加空状态，并为旧项目提供 `general`/未分类兜底分组。
- [x] 3.4 增加目录、分类标签、项目类型和业务模板字段的本地化文案。

## 4. Lark Skill 交接契约

- [x] 4.1 定义 skill 产出的 Lark 资料在 `raw/sources/lark/<resource-slug>/` 下的目录布局。
- [x] 4.2 定义项目本地来源清单：`.llm-wiki/sources/lark-sources.jsonl`。
- [x] 4.3 增加轻量来源清单类型与校验辅助函数，但不执行或配置 Lark CLI。
- [x] 4.4 验证资料监控器会把 `raw/sources/lark/` 下的文件视为普通可知识提取项目资料。
- [x] 4.5 确保应用不增加 Lark 连接器 UI，不解析 Lark CLI，也不执行 Lark CLI。

## 5. 后端/API 兼容

- [x] 5.1 如有必要，更新项目 list/open/create 响应处理，使前端能返回或补齐项目元数据。
- [x] 5.2 保持现有 `/api/v1/projects/*` 兼容性，继续支持只依赖 id、name、path 的客户端。
- [x] 5.3 为平台元数据出现前创建的项目增加迁移安全默认值。

## 6. 验证

- [x] 6.1 增加分类分组、项目元数据归一化和模板选择的单元测试。
- [x] 6.2 增加 Lark 来源清单校验测试，以及无效 skill 输出的非破坏性处理测试。
- [x] 6.3 增加资料监控器或资料生命周期测试，证明 `raw/sources/lark/` 下的合规文件会进入现有知识提取路径。
- [x] 6.4 运行平台目录和项目创建相关的前端定向测试。
- [x] 6.5 如果后端响应结构变化，运行项目 list/open/create 兼容性后端测试。
- [x] 6.6 运行构建或类型检查，确认元数据改动不破坏现有项目工作流。
